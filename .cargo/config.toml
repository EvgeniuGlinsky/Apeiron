# Rust build directories grow fast. If the system drive is cramped, keep the
# project and CARGO_HOME on another volume: target/ is created next to the
# project, and an absolute path is not needed here — it would not carry over
# between machines anyway.
[build]
target-dir = "target"
