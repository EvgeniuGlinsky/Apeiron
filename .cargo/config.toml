# Каталоги сборки Rust растут быстро. Если системный диск тесен, держите
# проект и CARGO_HOME на другом томе: target/ создаётся рядом с проектом,
# и абсолютный путь здесь не нужен — он всё равно не переносится между машинами.
[build]
target-dir = "target"
