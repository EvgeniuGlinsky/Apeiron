# Rust
/target/
**/target/
*.pdb

# Flutter / Dart
app/.dart_tool/
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
app/pubspec.lock
app/android/.gradle/
app/android/local.properties
app/android/app/debug/
app/android/app/profile/
app/android/app/release/
app/windows/flutter/ephemeral/
app/rust_builder/**/ephemeral/

# IDE
.idea/
*.iml
.vscode/
*.swp

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Secrets and keys — nothing should end up here, but just in case.
# Leaking a signing key through git is a classic way to lose a project.
*.jks
*.keystore
*.p12
*.pem
*.key
key.properties
secrets/
*.env
.env.*

# Handoff note: contains local paths and machine details,
# does not go into the public repository.
HANDOFF.md
