<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network, from this build on (R-012). Used for now only by the DHT
         measurement on the self-check screen: test envelopes of random bytes.
         What it costs, said plainly: nodes of the DHT near an envelope's address
         see the IP address of whoever puts or fetches it. There is still no
         server of ours anywhere. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="Apeiron"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!-- Backup is disabled deliberately, and in two ways at once.

             The master key lives in the device's hardware store and is never
             exported — so the backup would carry the encrypted database but not
             its key. A restore would yield undecryptable garbage, and a copy of
             the ciphertext would stay lying in the cloud.

             allowBackup="false" closes cloud backup and works on API 28-30.
             On targetSdk 31+ it alone is not enough: on some firmware it does not
             disable transfer to a new phone. That is why dataExtractionRules sits
             next to it, excluding both sections — cloud-backup and device-transfer. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
